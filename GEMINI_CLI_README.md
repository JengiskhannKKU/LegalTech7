# 🤖 Gemini CLI สำหรับ LandGuard Platform

## 📋 การติดตั้ง

1. **รับ API Key จาก Google**

   - ไปที่: https://makersuite.google.com/app/apikey
   - สร้าง API key ใหม่
   - คัดลอก API key

2. **ตั้งค่า Environment Variable**

   **Windows PowerShell:**

   ```powershell
   $env:GEMINI_API_KEY="your-api-key-here"
   ```

   **Windows CMD:**

   ```cmd
   set GEMINI_API_KEY=your-api-key-here
   ```

   **Linux/Mac:**

   ```bash
   export GEMINI_API_KEY=your-api-key-here
   ```

   **หรือใช้ไฟล์ .env:**

   ```bash
   cp .env.example .env
   # แก้ไขไฟล์ .env และใส่ API key ของคุณ
   ```

## 🚀 วิธีใช้งาน

### โหมดปกติ (รอผลลัพธ์ทั้งหมด)

```bash
npm run gemini "คำถามของคุณ"
```

### โหมด Stream (แสดงผลแบบ real-time)

```bash
npm run gemini-stream "คำถามของคุณ"
```

## 📚 ตัวอย่างการใช้งาน

### 1. ถามคำถามทั่วไป

```bash
npm run gemini "อธิบายเกี่ยวกับ Next.js App Router"
```

### 2. เขียนโค้ด

```bash
npm run gemini "เขียน React component สำหรับแสดงรายการที่ดิน"
```

### 3. วิเคราะห์โค้ด

```bash
npm run gemini "วิเคราะห์โค้ดนี้และแนะนำการปรับปรุง: function example() { ... }"
```

### 4. แปลภาษา

```bash
npm run gemini "แปลข้อความนี้เป็นภาษาอังกฤษ: สวัสดีครับ"
```

### 5. สรุปข้อความ

```bash
npm run gemini-stream "สรุปเนื้อหาเรื่อง AI และ Machine Learning ให้หน่อย"
```

## ⚙️ คุณสมบัติ

- ✅ รองรับภาษาไทย
- ✅ แสดงผลแบบ real-time (stream mode)
- ✅ แสดงผลแบบรอทั้งหมด (normal mode)
- ✅ รองรับคำถามยาว
- ✅ จัดการ error อย่างชัดเจน

## 🔧 การใช้งานใน Code

คุณสามารถนำไปใช้ในโค้ดของคุณได้:

```typescript
import { GoogleGenerativeAI } from "@google/generative-ai";

const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY!);
const model = genAI.getGenerativeModel({ model: "gemini-pro" });

// ใช้งาน
const result = await model.generateContent("คำถามของคุณ");
console.log(result.response.text());
```

## 📝 หมายเหตุ

- ต้องมี internet connection
- ต้องมี API key ที่ถูกต้อง
- อาจมี rate limit ตาม plan ของคุณ
- รองรับ Gemini Pro model

## 🆘 แก้ปัญหา

### ไม่มี API Key

```
❌ กรุณาตั้งค่า GEMINI_API_KEY environment variable
```

**แก้ไข:** ตั้งค่า environment variable ตามขั้นตอนด้านบน

### API Key ไม่ถูกต้อง

```
❌ เกิดข้อผิดพลาด: Invalid API key
```

**แก้ไข:** ตรวจสอบ API key ของคุณที่ https://makersuite.google.com/app/apikey

### Rate Limit

```
❌ เกิดข้อผิดพลาด: Resource exhausted
```

**แก้ไข:** รอสักครู่แล้วลองใหม่ หรืออัพเกรด plan

## 🔗 ลิงก์ที่เป็นประโยชน์

- [Google AI Studio](https://makersuite.google.com/)
- [Gemini API Documentation](https://ai.google.dev/docs)
- [Get API Key](https://makersuite.google.com/app/apikey)
